import React from "react";

/*
The closed sidebar containing buttons to show and their onClicks
This should extend to the height of the parent.
 */
export const ClosedSidebar: React.FC = () => {
    return null;
}